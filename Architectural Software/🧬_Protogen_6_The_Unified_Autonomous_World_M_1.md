# 🧬 Protogen V4.0.6: The Unified Autonomous World Model

[![License: MIT](https://img.shields.io/badge/License-MIT-yellow.svg)](https://opensource.org/licenses/MIT)
[![Python: 3.10+](https://img.shields.io/badge/Python-3.10+-blue.svg)](https://www.python.org/)
[![Hardware: AMD GPU Optimized](https://img.shields.io/badge/Hardware-AMD%20GPU%20Optimized-orange.svg)](https://github.com/microsoft/DirectML)

**Protogen** is an experimental "Digital Organism" designed to move beyond the limitations of traditional Large Language Models. While standard AI predicts the next word, Protogen builds a **persistent, evolving World Model** using graph theory, entropy-based filtering, and recursive logic synthesis.

---

## 🌟 Why Protogen is Different

Most AI today is a "Black Box"—you put text in, and you get text out, but you don't know *why*. Protogen is a **White-Box** system:

*   **Metabolic Heartbeat:** The system has an internal "pulse." It processes, cleans, and organizes its own thoughts in the background, even when you aren't interacting with it.
*   **Entropy Gating:** It uses **Shannon Entropy** to protect its "sanity." If you feed it nonsense, it detects the chaos and refuses to let it corrupt its internal logic.
*   **Symbolic Evolution:** It watches how concepts relate. When a relationship becomes strong enough, it "lifts" that concept into a **Symbol**, creating a permanent foundation for future reasoning.
*   **Recursive Learning:** It doesn't just learn facts; it learns **rules**. It then looks for patterns *within those rules* to build increasingly complex understanding.

---

## 🚀 Quick Start

### 1. Prerequisites
You'll need Python 3.10 or higher. To install the core engine and document processors, run:

```bash
pip install pypdf python-docx networkx torch
```

### 2. Hardware Acceleration (AMD GPUs)
Protogen is optimized for **AMD RX 580** and similar hardware. To enable the GPU acceleration module:

```bash
pip install torch-directml
```

### 3. Running the Engine
Simply run the script to begin the **Genesis** event:

```bash
python protogen_4.0_6_unified.py
```

---

## 🧠 How to Use It

### **The Library System**
Drop any `.pdf`, `.docx`, or `.txt` files into the `/protogen_core/library` folder. Type `/sync` in the chat to have the Protogen "ingest" the knowledge. It will automatically filter out "noisy" data and integrate the rest into its logic map.

### **The Chat Interface**
Talk to the Protogen to explore its current associations. It will tell you exactly which concepts it is connecting and why.

### **Observing the "Brain"**
You can look inside the `protogen_core` folder at any time to see the system's internal state:
*   `ontology_sqt.json`: The current map of its world.
*   `audit_log.json`: A cryptographic record of every "mutation" and "thought" the system has had.
*   `reflection_export.txt`: A summary of the system's most important current symbols.

---

## 🗺️ Roadmap & Vision

The goal of the Protogen project is to achieve **Autonomous Machine Intelligence (AMI)**—AI that doesn't just mimic human speech, but understands the underlying structure of reality.

*   [x] **V4.0.6:** Unified Async Heartbeat & Entropy Gating.
*   [ ] **Next:** Implementation of "Metabolic Decay" (forgetting low-value info).
*   [ ] **Future:** Multi-agent Protogen synchronization.

---

## 🤝 Contributing

This is an open-source project. Whether you are a researcher in **Graph Theory**, an **AI Safety** advocate, or just a curious developer, we welcome your contributions! 

1. Fork the repo.
2. Create your feature branch (`git checkout -b feature/AmazingFeature`).
3. Commit your changes (`git commit -m 'Add some AmazingFeature'`).
4. Push to the branch (`git push origin feature/AmazingFeature`).
5. Open a Pull Request.

---

## 📄 License

Distributed under the MIT License. See `LICENSE` for more information.

**Built with 🧠 by AMI Labs.**
